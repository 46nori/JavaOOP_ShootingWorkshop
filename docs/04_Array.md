# Step 4: 多数の敵の生成と制御

よりゲームらしくするために、複数の敵を登場させ、数学を使って動きを複雑にしてみましょう。
`src/step04_array` パッケージを使います。

## 4-1. 移動アルゴリズムの変更 (`Enemy.java`)

```java
public void move() {
    // 角度(度数法)をラジアンに変換
    double rad = Math.toRadians(angle);
    
    // 三角関数で成分分解し、移動距離(speed)を掛けて、次の表示位置(座標)を更新
    x += Math.cos(rad) * speed;
    y += Math.sin(rad) * speed;

    // 画面端での跳ね返りなどを追加しても良いですね
    if (x > 800) x = 0;
    if (y > 600) y = 0;
}
```

## 4-2. 複数オブジェクトの生成 (`GameMain.java`)

```java
// 配列の確保
Enemy[] enemies = new Enemy[10];

// 各要素のインスタンス化
for (int i = 0; i < enemies.length; i++) {
    double startX = Math.random() * 800;
    double startY = Math.random() * 600;
    double angle = Math.random() * 360;
    double speed = 1.0 + Math.random() * 3.0;
    
    enemies[i] = new Enemy(startX, startY, angle, speed);
}
```

> **🎲 Math.random() とは？**
> `Math.random()` は、**0.0 以上 1.0 未満** のランダムな小数を返す関数です。
> これを使って、欲しい範囲の乱数を作ることができます。
>
> - **`Math.random() * 800`**
>   - 0.0 〜 0.999... の値に 800 を掛けるので、**0.0 〜 799.9...** の範囲になります。
>   - これで画面の横幅（0〜800）のどこかにランダムに配置できます。
>
> - **`1.0 + Math.random() * 3.0`**
>   - `Math.random() * 3.0` は **0.0 〜 2.999...** になります。
>   - これに 1.0 を足すので、結果は **1.0 〜 3.999...** になります。
>   - つまり、「最低でもスピード1.0、最大でスピード4.0近く」という調整ができます。

## 実行方法

Eclipse上で `src/step04_array/GameMain.java` を右クリックし、**[Run As]** > **[Java Application]** を選択します。
たくさんの敵がランダムな方向に動いていれば成功です！

次は [Step 5: 継承 (Inheritance)](05_Inheritance.md) です。
